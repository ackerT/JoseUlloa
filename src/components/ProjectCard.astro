---
const { title, shortDescription, slug } = Astro.props;
---
<a href={`/proyecto-${slug}`} class="block bg-[#FBF1DC] text-[#33595E] p-6 rounded-2xl shadow-lg hover:shadow-2xl hover:scale-105 transition-transform duration-300">
  <h3 class="text-2xl font-bold mb-2">{title}</h3>
  <p class="text-lg">{shortDescription}</p>
</a>

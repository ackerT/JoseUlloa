<header class="w-full p-6 flex flex-col md:flex-row justify-between items-center bg-[#33595E] text-[#FDF1DD]">
    <div class="w-full flex justify-between items-center">
        <a href="/" class="text-2xl font-bold">Acker</a>
    </div>
    <nav class="w-full md:flex md:justify-end md:items-center mt-4 md:mt-0">
        <a 
        class={`mx-2 block transition-all ${Astro.url.pathname === '/' || Astro.url.pathname === '/sobremi' ? 'border-b-2 border-current' : ''}`}
        href="/#mas-sobre-mi"
    >
        Sobre Mí
    </a>
        <a 
            class={`mx-2 block transition-all ${
                Astro.url.pathname === '/proyectos/' || Astro.url.pathname === '/proyectos' 
                    ? 'border-b-2 border-current' 
                    : ''
            }`}
            href="/proyectos"
        >
            Proyectos
        </a>
    </nav>
</header>
